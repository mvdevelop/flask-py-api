
services:
  - type: web
    name: pystore-api
    env: docker
    plan: free
    region: oregon

    dockerfilePath: ./Dockerfile

    healthCheckPath: /health

    envVars:
      - key: MONGO_URI
        sync: false

      - key: MONGO_DB
        value: py_store
